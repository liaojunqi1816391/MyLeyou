server:
  port: 8086
spring:
  application:
    name: sms-service
  redis:
    host: localhost
  rabbitmq:
    host: localhost
    username: leyou
    password: leyou
    virtual-host: /leyou
leyou:
  sms:
    accessKeyId:  # 你自己的accessKeyId
    accessKeySecret:  # 你自己的AccessKeySecret
    signName:  # 签名名称
    verifyCodeTemplate:  # 模板名称
